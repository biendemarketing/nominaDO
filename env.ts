// env.ts
// This is an example of the environment variables file.
// For local development, create a file named `env.local.ts`
// with your actual API key. This `env.ts` file can serve as a template.
// The `env.local.ts` file should be added to your .gitignore in a real project.

export const process = {
  env: {
    API_KEY: 'YOUR_GEMINI_API_KEY_HERE'
  }
};
